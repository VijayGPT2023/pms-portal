# ============================================================================
# PMS Portal requirements -- pinned for DirectAdmin shared hosting (Python 3.12)
# ============================================================================
# DO NOT add: mysqlclient, psycopg2, psycopg2-binary, cryptography, gunicorn,
#             uwsgi, gevent -- all fail to compile on the server's old GCC.
# Passenger is the WSGI server; pymysql is the pure-Python MySQL driver.
# ============================================================================

# --- Django stack ----------------------------------------------------------
Django==6.0.3
django-htmx==1.27.0
django-fsm==3.0.1
django-auditlog==3.4.1
django-widget-tweaks==1.5.1
whitenoise==6.12.0

# --- Database driver (pure Python, no compilation) -------------------------
pymysql==1.1.1

# --- Data processing (pinned to versions with pre-built wheels for 3.12) ---
numpy==1.26.4
pandas==2.2.3
openpyxl==3.1.5
xlrd==2.0.2

# --- Supporting libs --------------------------------------------------------
python-dateutil==2.9.0.post0
pytz==2025.2
greenlet==3.1.1
